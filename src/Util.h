#pragma once

#include <string>

namespace Util {
        std::string getInput();
        void print(std::string str);
        void println(std::string str);
        int now();
        int calcDays(int date);
};
